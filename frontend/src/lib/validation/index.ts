/**
 * Validation foundation. A schema library is intentionally NOT added in
 * the foundation step. Concrete form schemas will be introduced in the
 * feature steps that need them, after the backend contract is read.
 */
export {};
