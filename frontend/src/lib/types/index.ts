/**
 * Shared TypeScript types. Domain-specific shapes (orders, returns, etc.)
 * are added in their respective feature steps once the backend contract
 * is read directly from the FastAPI handlers.
 *
 * This barrel is intentionally empty for the foundation step.
 */
export {};
